language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t hugofqueiros/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true hugofqueiros/docker-react npm run test -- --coverage
